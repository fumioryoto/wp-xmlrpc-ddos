# wp-xmlrpc-ddos
This repository contains a legacy proof-of-concept (PoC) script demonstrating an XML entity expansion–based Denial of Service (DDoS) condition against WordPress and Drupal XML-RPC endpoints that relied on unsafe XML parsing behavior.
